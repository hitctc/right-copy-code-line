# 协作开发规则

## 开发结构

- 代码必须保持结构化、模块化，按职责拆分文件，禁止把全部业务逻辑集中在一个文件中。
- 命令入口只负责编排流程；选区处理、路径解析、文本格式化和外部 API 适配分别放在独立模块中。
- 修改前先确认要修改的文件，并用 3-6 条要点说明计划。
- 只修改与当前需求直接相关的代码，不顺手重构无关部分。
- 新增或修改的方法需要补充自然、可读的注释；关键路径、边界条件和复杂分支附近也要说明原因。

## 当前产品边界

- 只实现单段连续选区的代码上下文复制。
- 输出内容依次为工作区相对路径、完整起止行范围和完整起止行代码片段。
- 不使用 Markdown 代码块或其他特殊格式。
- 没有工作区、文件未保存或没有选区时，不复制代码；没有选区时菜单项保持禁用。
- 使用独立的“复制代码上下文”菜单项，不覆盖 VS Code 原生“复制”。
- 暂不实现目录复制、项目结构、Token 统计、多格式导出和多选区合并。

## 提交与推送规则

- 开发完成后使用 `ship commit push` 完成交付。
- 交付阶段只执行：只读 diff、写提交描述、`git commit`、`git push`。
- 类型检查或探查只在 diff 看不懂，或改动确实触及类型/接口时执行。
- 提交前必须能清楚说明实际改动范围、验证结果和残余风险；不得把未执行的动作写成已完成。
- 提交信息使用中文摘要，正文包含具体变更说明和“变更文件:”清单，不添加身份署名 trailer。
